package #{project.package}.factory;

import #{domain.package}#{domain.class};

import org.romaframework.core.factory.AbstractFactory;

/**
 * Factory class for #{domain.class} entity. It's registered in the IoC configuration files and is used by Roma components to create objects of kind #{domain.class}. 
 * <br/>Use EntityHelper.createObject() to create objects using the its factory.
 * <br/>This class was generated by Roma Meta Framework CRUD wizard.
 * @see EntityHelper  
 */ 
public class #{crud.class}Factory extends AbstractFactory<#{crud.class}> {
  @Override
  public #{domain.class} create() {
    return new #{domain.class}();
  }
}
